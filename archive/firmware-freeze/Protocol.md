# Protocol Documentation

The authoritative firmware protocol specification is `docs/PROTOCOL_SPEC.md`.

The old ASCII protocol draft is obsolete. Current firmware implements a binary USB packet protocol with command IDs listed in `docs/PROTOCOL_REGISTRY.md` and frozen for host development in `docs/FIRMWARE_FREEZE.md`.
