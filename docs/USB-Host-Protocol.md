# USB Host Protocol

Use `docs/PROTOCOL_SPEC.md`, `docs/PROTOCOL_REGISTRY.md`, and `docs/FIRMWARE_FREEZE.md` for host implementation.

Current firmware expects one complete binary packet per non-empty USB receive operation and sends one binary response packet.
