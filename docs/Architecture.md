# Architecture Documentation

The synchronized architecture reference is `docs/ARCHITECTURE.md`.

This file intentionally defers to that freeze-era document to avoid duplicate architecture descriptions drifting away from firmware implementation.
